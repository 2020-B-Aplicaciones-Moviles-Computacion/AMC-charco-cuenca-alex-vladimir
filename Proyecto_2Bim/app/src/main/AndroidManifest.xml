<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto_2bim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Cheems Project"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_2Bim">
        <activity android:name=".ComicChapter"></activity>
        <activity android:name=".ComicDetail" />
        <activity android:name=".Authentication" />
        <activity android:name=".Manga" />
        <activity android:name=".Library" />
        <activity android:name=".Settings" />
        <activity android:name=".Favoritos" />
        <activity android:name=".Search" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>